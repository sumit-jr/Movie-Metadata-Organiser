<div class="flex flex-col w-full items-center justify-start">
  <p style="color: green"><%= notice %></p>

  <%= render @admin_movie %>

  <div class="mt-4 flex gap-4">
    <%= link_to "Back to movies", admin_movies_path %>
    <%= button_to "Destroy this movie", [:admin, @admin_movie], method: :delete, data: {'turbo-confirm': "Are you sure?" } %>
  </div>
</div>